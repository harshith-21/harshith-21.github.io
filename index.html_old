<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Harshith portfolio </title>
  <!-- Funky font from Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Edu+NSW+ACT+Foundation&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="https://avatars.githubusercontent.com/u/86101160?v=4" />
  <style>
    :root {
      --color-bg-dark: #181024;
      --color-primary-dark: #7c4dff;
      --color-text-dark: #fff;
    }
    html {
      color-scheme: dark;
    }
    body {
      margin: 0;
      font-family: 'Inter', Arial, sans-serif;
      background: var(--color-bg-dark);
      color: var(--color-text-dark);
      transition: background 0.3s, color 0.3s;
    }
    .navbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 2rem;
      background: none;
      box-shadow: none;
      backdrop-filter: none;
      transition: background 0.3s;
      z-index: 10;
    }
    .navbar .logo {
      font-family: 'Edu NSW ACT Foundation', cursive;
      font-size: 2rem;
      letter-spacing: 2px;
      color: #fff;
      user-select: none;
    }
    .theme-toggle {
      background: none;
      border: none;
      cursor: pointer;
      font-size: 1.7rem;
      color: #fff;
      transition: color 0.3s;
      outline: none;
    }
    .main-content {
      display: flex;
      flex-direction: row;
      width: 90vw;
      height: 90vh;
      gap: 2vw;
      padding: 3vw;
      box-sizing: border-box;
      margin: 0 auto;
      position: relative;
    }
    .side-box {
      border-radius: 1.5rem;
      box-shadow:
        0 4px 24px 0 rgba(60, 30, 90, 0.18), /* main shadow */
        0 0 60px 10px rgba(124, 77, 255, 0.25), /* purple glow */
        0 0 120px 30px rgba(124, 77, 255, 0.10); /* soft outer glow */
      background: rgba(33, 0, 125, 0.12);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-width: 0;
      min-height: 0;
      transition: background 0.3s;
      z-index: 1;
    }
    .left-box {
      flex: 0 0 320px;
      max-width: 350px;
      min-width: 260px;
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      padding: 2rem 1rem 1.5rem 1rem;
      min-height: 220px;
      box-sizing: border-box;
      gap: 1.1rem;
      overflow: hidden;
    }
    .left-box > .profile-pic {
      flex-shrink: 0;
      flex-grow: 0;
      max-width: 120px;
      min-width: 60px;
      width: 60%;
      margin: 0 auto 0.7rem auto;
      align-self: center;
    }
    .left-box > .profile-text {
      width: 100%;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      flex: 1 1 auto;
      min-width: 0;
      overflow: hidden;
      word-break: break-word;
    }
    .left-box .profile-socials {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1.2rem;
      margin-top: 1.1rem;
      width: 100%;
      overflow: visible; /* Prevent cropping */
    }
    .right-box {
      flex: 1 1 0;
      align-items: stretch;
      justify-content: flex-start;
      padding-top: 0;
      min-width: 0;
    }
    /* CSS for profile image */
    .profile-pic {
      width: 60%;
      max-width: 180px;
      aspect-ratio: 1/1;
      border-radius: 50%;
      object-fit: cover;
      box-shadow: 0 2px 12px 0 rgba(60, 30, 90, 0.10);
      margin: 0 0 1.2rem 0;
      border: 4px solid rgba(124, 77, 255, 0.18);
      background: #fff;
      transition: border 0.3s;
    }
    .profile-text {
      width: 100%;
      text-align: center;
      display: flex;
      flex-direction: column;
      gap: 0.3rem;
    }
    .profile-name {
      font-size: 1.25rem;
      font-weight: 600;
      letter-spacing: 0.5px;
      margin-bottom: 0.2rem;
    }
    .profile-role {
      font-size: 1rem;
      color: #7c4dff;
      font-weight: 500;
      margin-bottom: 0.1rem;
    }
    .profile-edu, .profile-city {
      font-size: 0.95rem;
      color: #888;
      font-weight: 400;
    }
    /* Social icons styling */
    .profile-socials {
      display: flex;
      justify-content: center;
      gap: 1.2rem;
      margin-top: 1.1rem;
    }
    .social-link {
      display: flex;
      align-items: center;
      text-decoration: none;
    }
    .social-img {
      width: 2.2rem;
      display: inline-block;
      vertical-align: middle;
      aspect-ratio: 1/1;
      border-radius: 0.4rem;
      background: #fff;
      transition: transform 0.2s, background 0.2s;
      box-shadow: 0 0 0 0.5px #eee; /* Thinner border */
      padding: 0.15rem;
      overflow: visible;
    }
    .social-link:hover .social-img {
      transform: scale(1.12);
      box-shadow: 0 0 0 2px #b39ddb;
    }
    /* Terminal bar styles */
    .terminal-bar {
      width: 100%;
      height: 2.2rem;
      background: rgba(30, 20, 40, 0.92);
      border-top-left-radius: 1.5rem;
      border-top-right-radius: 1.5rem;
      display: flex;
      align-items: center;
      padding-left: 1.1rem;
      box-sizing: border-box;
      position: relative;
      margin-bottom: 0.5rem;
      justify-content: space-between;
      padding-right: 1.1rem;
    }
    .traffic-lights {
      display: flex;
      gap: 0.5rem;
      align-items: center;
    }
    .light {
      width: 0.85rem;
      height: 0.85rem;
      border-radius: 50%;
      display: inline-block;
      box-shadow: 0 0 2px 0 rgba(0,0,0,0.12);
      border: 1.5px solid rgba(0,0,0,0.08);
    }
    .light.red { background: #ff5f56; border-color: #e0443e; }
    .light.yellow { background: #ffbd2e; border-color: #dea123; }
    .light.green { background: #27c93f; border-color: #13a10e; }
    .terminal-time {
      font-family: 'Menlo', 'Monaco', 'Consolas', monospace;
      font-size: 1.05rem;
      color: #e0e0e0;
      letter-spacing: 0.04em;
      opacity: 0.85;
      user-select: none;
    }
    .terminal-content {
      flex: 1;
      font-family: 'Menlo', 'Monaco', 'Consolas', monospace;
      font-size: 1.08rem;
      color: #e0e0e0;
      background: transparent;
      min-height: 120px;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: flex-start;
      padding: 0;
      overflow-y: auto;
      max-height: 100%;
    }
    .terminal-line {
      display: flex;
      align-items: center;
      min-height: 1.6em;
      white-space: pre;
    }
    .terminal-prompt {
      color: #b39ddb;
      font-weight: 500;
    }
    .terminal-user {
      color: #7c4dff;
    }
    .terminal-host {
      color: #fff;
    }
    .terminal-path {
      color: #ffd54f;
    }
    @media (max-width: 900px) {
      .main-content {
        flex-direction: column;
        height: auto;
        gap: 2vw;
        padding: 4vw 2vw;
        width: 100%;
        top: 0;
      }
      .left-box, .right-box {
        width: 100%;
        min-width: 0;
        min-height: 120px;
        height: auto;
        margin-bottom: 0;
        max-width: 100%;
      }
      .left-box {
        order: 1;
        padding: 2rem 1rem 1.5rem 1rem;
        gap: 1.1rem;
        min-height: 180px;
      }
      .right-box {
        order: 2;
        margin-top: 0.7rem;
        min-height: 220px;
        padding: 0.5rem 0.2rem 1.2rem 0.2rem;
      }
    }
    @media (max-width: 600px) {
      .main-content {
        padding: 2vw 0.5vw;
        gap: 1vw;
      }
      .left-box {
        padding: 1rem 0.2rem 1rem 0.2rem;
        min-height: 120px;
        gap: 0.7rem;
      }
      .left-box > .profile-pic {
        max-width: 60px;
      }
      .left-box > .profile-text {
        font-size: 0.98rem;
      }
      .left-box .profile-socials {
        gap: 0.7rem;
        margin-top: 0.5rem;
      }
      .right-box {
        padding: 0.2rem 0 1rem 0;
        min-height: 140px;
      }
    }
    @media (max-width: 400px) {
      .main-content {
        padding: 1vw 0.2vw;
      }
      .left-box {
        padding: 0.7rem 0.1rem 0.7rem 0.1rem;
        min-height: 80px;
        gap: 0.3rem;
      }
      .left-box > .profile-pic {
        max-width: 40px;
      }
      .left-box > .profile-text {
        font-size: 0.92rem;
      }
      .right-box {
        min-height: 80px;
      }
    }
    @media (max-width: 600px) {
      .profile-pic {
        width: 90%;
        max-width: 120px;
        margin: 0 0 1rem 0;
      }
    }
    @media (max-width: 1200px) and (min-width: 901px) {
      .main-content {
        flex-direction: row;
        height: auto;
        gap: 2vw;
        padding: 4vw 2vw;
        width: 100%;
        top: 0;
        justify-content: flex-end;
      }
      .left-box {
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 40%;
        min-width: 260px;
        gap: 1.1rem;
        padding: 2rem 1.5rem 1.5rem 1.5rem;
        margin-left: auto;
        margin-right: 0;
      }
      .left-box > .profile-pic {
        width: 90px;
        min-width: 70px;
        max-width: 120px;
        margin: 0 auto 0.7rem auto;
        align-self: center;
      }
      .left-box > .profile-text {
        align-items: center;
        text-align: center;
        gap: 0.5rem;
        flex: 1 1 0;
        min-width: 0;
        overflow: hidden;
        word-break: break-word;
        font-size: 1.08rem;
      }
      .left-box .profile-socials {
        justify-content: center;
        margin-top: 1.2rem;
        gap: 1.2rem;
      }
      .right-box {
        width: 60%;
        min-width: 0;
        margin-left: 0;
        margin-right: auto;
        min-height: 320px;
        padding: 0.5rem 0.2rem 1.2rem 0.2rem;
        display: flex;
        flex-direction: column;
      }
    }
    /* --- Tablet: stacked, profile left/text right in left-box --- */
    @media (max-width: 1200px) and (min-width: 701px) {
      .main-content {
        flex-direction: column;
        width: 100vw;
        height: auto;
        gap: 2vw;
        padding: 4vw 2vw;
      }
      .left-box {
        display: flex;
        flex-direction: row;
        align-items: stretch;
        justify-content: center;
        width: 100%;
        min-width: 0;
        min-height: 180px;
        gap: 0;
        padding: 2rem 1.5rem 1.5rem 1.5rem;
        max-width: none;
        margin: 0;
      }
      .left-box > .profile-pic {
        flex: 0 0 40%;
        max-width: none;
        min-width: 0;
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0;
      }
      .left-box > .profile-pic img,
      .left-box > img.profile-pic {
        display: block;
        margin: 0 auto;
        width: 120px;
        max-width: 150px;
        min-width: 90px;
        aspect-ratio: 1/1;
      }
      .left-box > .profile-text {
        flex: 0 0 60%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        gap: 0.5rem;
        min-width: 0;
        overflow: hidden;
        word-break: break-word;
        font-size: 1.08rem;
        padding-left: 2vw;
        padding-right: 2vw;
      }
      .left-box .profile-socials {
        justify-content: center;
        margin-top: 1.2rem;
        gap: 1.2rem;
      }
      .right-box {
        margin-top: 1.5rem;
        min-height: 320px;
        padding: 0.5rem 0.2rem 1.2rem 0.2rem;
        width: 100%;
      }
    }
    /* Remove all conflicting .left-box rules in this band */
    @media (max-width: 1200px) and (min-width: 701px) {
      .left-box {
        flex: unset !important;
        max-width: none !important;
        min-width: 0 !important;
        width: 100% !important;
        margin: 0 !important;
      }
    }
    /* Remove desktop/tablet .left-box width/flex/max-width in this band */
    @media (max-width: 1200px) and (min-width: 701px) {
      .left-box {
        flex-basis: unset !important;
      }
    }
    @media (max-width: 600px) {
      .profile-pic {
        width: 90%;
        max-width: 120px;
        margin: 0 0 1rem 0;
      }
    }
    @media (max-width: 1200px) and (min-width: 901px) {
      .main-content {
        flex-direction: row;
        height: auto;
        gap: 2vw;
        padding: 4vw 2vw;
        width: 100%;
        top: 0;
        justify-content: flex-end;
      }
      .left-box {
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 40%;
        min-width: 260px;
        gap: 1.1rem;
        padding: 2rem 1.5rem 1.5rem 1.5rem;
        margin-left: auto;
        margin-right: 0;
      }
      .left-box > .profile-pic {
        width: 90px;
        min-width: 70px;
        max-width: 120px;
        margin: 0 auto 0.7rem auto;
        align-self: center;
      }
      .left-box > .profile-text {
        align-items: center;
        text-align: center;
        gap: 0.5rem;
        flex: 1 1 0;
        min-width: 0;
        overflow: hidden;
        word-break: break-word;
        font-size: 1.08rem;
      }
      .left-box .profile-socials {
        justify-content: center;
        margin-top: 1.2rem;
        gap: 1.2rem;
      }
      .right-box {
        width: 60%;
        min-width: 0;
        margin-left: 0;
        margin-right: auto;
        min-height: 320px;
        padding: 0.5rem 0.2rem 1.2rem 0.2rem;
        display: flex;
        flex-direction: column;
      }
    }
    /* --- Tablet: stacked, profile left/text right in left-box --- */
    @media (max-width: 1200px) and (min-width: 701px) {
      .main-content {
        flex-direction: column;
        width: 100vw;
        height: auto;
        gap: 2vw;
        padding: 4vw 2vw;
      }
      .left-box {
        display: flex;
        flex-direction: row;
        align-items: stretch;
        justify-content: center;
        width: 100%;
        min-width: 0;
        min-height: 180px;
        gap: 0;
        padding: 2rem 1.5rem 1.5rem 1.5rem;
        max-width: none;
        margin: 0;
      }
      .left-box > .profile-pic {
        flex: 0 0 40%;
        max-width: none;
        min-width: 0;
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0;
      }
      .left-box > .profile-pic img,
      .left-box > img.profile-pic {
        display: block;
        margin: 0 auto;
        width: 120px;
        max-width: 150px;
        min-width: 90px;
        aspect-ratio: 1/1;
      }
      .left-box > .profile-text {
        flex: 0 0 60%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        gap: 0.5rem;
        min-width: 0;
        overflow: hidden;
        word-break: break-word;
        font-size: 1.08rem;
        padding-left: 2vw;
        padding-right: 2vw;
      }
      .left-box .profile-socials {
        justify-content: center;
        margin-top: 1.2rem;
        gap: 1.2rem;
      }
      .right-box {
        margin-top: 1.5rem;
        min-height: 320px;
        padding: 0.5rem 0.2rem 1.2rem 0.2rem;
        width: 100%;
      }
    }
    /* --- Mobile: stacked, image above text --- */
    @media (max-width: 700px) {
      .main-content {
        flex-direction: column;
        width: 100vw;
        height: auto;
        gap: 1vw;
        padding: 2vw 0.5vw;
      }
      .left-box {
        width: 90vw !important;
        max-width: 90vw !important;
        min-width: 0 !important;
        margin: 0 auto 0 auto !important;
        border-radius: 1.5rem !important;
        padding: 1.2rem 1.1rem 1.1rem 1.1rem !important;
        background: rgba(33, 0, 125, 0.12);
        box-shadow:
          0 4px 24px 0 rgba(60, 30, 90, 0.18),
          0 0 60px 10px rgba(124, 77, 255, 0.25),
          0 0 120px 30px rgba(124, 77, 255, 0.10);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 0.7rem;
        min-height: 120px;
        overflow: visible;
      }
      .right-box {
        width: 90vw !important;
        max-width: 90vw !important;
        min-width: 0 !important;
        margin: 1.2rem auto 0 auto !important;
        border-radius: 1.5rem !important;
        box-shadow:
          0 4px 24px 0 rgba(60, 30, 90, 0.18),
          0 0 60px 10px rgba(124, 77, 255, 0.25),
          0 0 120px 30px rgba(124, 77, 255, 0.10);
        background: rgba(33, 0, 125, 0.12);
      }
      .slide-below-inner {
        flex-direction: column !important;
        align-items: stretch !important;
        justify-content: flex-start !important;
        height: auto !important;
        min-height: 0 !important;
        gap: 1.2rem !important;
        padding: 2vw 0.5vw 5vw 0.5vw !important;
      }
      .slide-below-inner .side-box {
        width: 100% !important;
        max-width: 100% !important;
        min-width: 0 !important;
        margin: 0 0 0 0 !important;
        border-radius: 1.5rem !important;
        box-sizing: border-box !important;
      }
      .snap-container {
        scroll-snap-type: none !important;
        height: auto !important;
        overflow-y: visible !important;
      }
      .slide-below,
      .slide-below-inner {
        width: 100vw !important;
        min-width: 0 !important;
        max-width: 100vw !important;
        height: auto !important;
        min-height: 0 !important;
        display: block !important;
        background: none !important;
        box-shadow: none !important;
        padding: 0 !important;
        margin: 0 !important;
      }
      .slide-below-inner {
        gap: 0 !important;
        padding: 0 0 2vw 0 !important;
      }
      .slide-below-inner .side-box {
        width: 94vw !important;
        max-width: 98vw !important;
        margin: 1.2rem auto !important;
        border-radius: 1.5rem !important;
        box-sizing: border-box !important;
        display: flex !important;
        flex-direction: column !important;
        align-items: center !important;
        justify-content: flex-start !important;
      }
      .scroll-tube {
        display: none !important;
      }
    }
    .snap-container {
      scroll-snap-type: y mandatory;
      height: 100vh;
      overflow-y: scroll;
      overflow-x: hidden;
      scrollbar-width: none; /* Firefox */
      -ms-overflow-style: none; /* IE/Edge */
      scroll-behavior: smooth; /* enable smooth snap */
      scroll-snap-stop: always;
    }
    .main-content, .slide-below {
      scroll-snap-align: start;
    }
    .slide-below {
      width: 100vw;
      height: 100vh;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--color-bg-dark);
      box-sizing: border-box;
      overflow: hidden;
    }
    .slide-below-inner {
      width: 90vw;
      height: 90vh;
      min-height: 90vh;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      box-sizing: border-box;
    }
    /* Tubelight scroll bar */
    .scroll-tube {
      position: fixed;
      right: 2vw;
      top: 50%;
      transform: translateY(-50%);
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 25vh;
      min-height: 80px;
      max-height: 200px;
      pointer-events: none;
    }
    .scroll-tube-bar {
      width: 10px;
      height: 100%;
      background: rgba(124,77,255,0.13);
      border-radius: 8px;
      box-shadow: 0 0 16px 4px rgba(124,77,255,0.18), 0 0 40px 10px rgba(124,77,255,0.10);
      position: relative;
      overflow: hidden;
      display: flex;
      align-items: flex-start;
      justify-content: center;
    }
    .scroll-tube-highlight {
      position: absolute;
      left: 0;
      width: 100%;
      height: 50%;
      border-radius: 8px;
      background: linear-gradient(180deg, #b39ddb 60%, #7c4dff 100%);
      box-shadow: 0 0 16px 4px #7c4dff99;
      transition: top 0.18s cubic-bezier(.77,0,.18,1), background 0.18s;
      top: 0;
    }
    .scroll-tube.highlight-bottom .scroll-tube-highlight {
      top: 50%;
      background: linear-gradient(0deg, #b39ddb 60%, #7c4dff 100%);
    }
    @media (max-width: 700px) {
      .scroll-tube {
        right: 1vw;
        height: 18vh;
        min-height: 50px;
        max-height: 100px;
      }
      .scroll-tube-bar {
        width: 7px;
      }
    }
  </style>
</head>
<body>
    <nav class="navbar">
      <div class="logo">HG</div>
      <button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">
        <!-- Icon will be set by JS -->
      </button>
    </nav>
    <div class="snap-container">
      <div class="main-content">
        <div class="side-box left-box">
          <img src="https://avatars.githubusercontent.com/u/86101160?v=4" alt="Profile" class="profile-pic" style="margin-top:0;margin-bottom:1.2rem;" />
          <div class="profile-text">
            <div class="profile-name">Harshith Gandhe</div>
            <div class="profile-role">Sr.SDE @ Acceldata</div>
            <div class="profile-edu">BITS PILANI</div>
            <div class="profile-city">BANGALORE</div>
            <div class="profile-socials">
              <a href="https://github.com/harshith-21" class="social-link" target="_blank" aria-label="GitHub">
                <!-- GitHub Mark (official, dark purple, no border, no cut) -->
                <svg class="social-img github-img" viewBox="0 0 32 32" width="35" height="35" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <rect width="32" height="32" rx="6" fill="#4B277A"/>
                  <path fill="#fff" d="M16 7.026c-4.97 0-9 4.03-9 9 0 3.98 2.58 7.35 6.16 8.54.45.08.62-.2.62-.44 0-.22-.01-.94-.01-1.7-2.51.46-3.04-.61-3.23-1.17-.1-.26-.54-1.17-.92-1.41-.32-.18-.78-.62-.01-.63.72-.01 1.23.66 1.4.94.82 1.38 2.13.99 2.65.76.08-.6.32-.99.58-1.22-2-.23-4.1-1-4.1-4.44 0-.98.35-1.78.93-2.41-.09-.23-.4-1.16.09-2.41 0 0 .75-.24 2.45.92.71-.2 1.47-.3 2.23-.3.76 0 1.52.1 2.23.3 1.7-1.16 2.45-.92 2.45-.92.49 1.25.18 2.18.09 2.41.58.63.93 1.43.93 2.41 0 3.45-2.1 4.21-4.1 4.44.33.28.62.83.62 1.68 0 1.22-.01 2.2-.01 2.5 0 .24.17.52.62.44A9.01 9.01 0 0 0 25 16.026c0-4.97-4.03-9-9-9Z"/>
                </svg>
              </a>
              <a href="https://www.linkedin.com/in/harshith-gandhe/" class="social-link" target="_blank" aria-label="LinkedIn">
                <!-- LinkedIn (official, centered, larger white 'in' on purple) -->
                <svg class="social-img" viewBox="0 0 32 32" width="35" height="35" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <rect width="32" height="32" rx="6" fill="#4B277A"/>
                  <g>
                    <text x="16" y="23" text-anchor="middle" font-family="Inter, Arial, sans-serif" font-size="17" font-weight="bold" fill="#fff" letter-spacing="-1">in</text>
                  </g>
                </svg>
              </a>
              <a href="https://medium.com/@gandheharshith21" class="social-link" target="_blank" aria-label="Medium">
                <!-- Medium (official, dark purple, no border, no cut) -->
                <svg class="social-img" viewBox="0 0 32 32" width="35" height="35" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <rect width="32" height="32" rx="6" fill="#4B277A"/>
                  <ellipse cx="10.5" cy="16" rx="4.5" ry="7" fill="#fff"/>
                  <ellipse cx="21.5" cy="16" rx="3.5" ry="7" fill="#fff"/>
                  <ellipse cx="16" cy="16" rx="2.5" ry="7" fill="#fff"/>
                </svg>
              </a>
            </div>
          </div>
        </div>
        <div class="side-box right-box">
          <div class="terminal-bar">
            <div class="traffic-lights">
              <span class="light red"></span>
              <span class="light yellow"></span>
              <span class="light green"></span>
            </div>
            <div class="terminal-time" id="terminalTime"></div>
          </div>
          <div class="terminal-content">
            <div class="terminal-line">
              <span class="terminal-prompt">&nbsp;<span class="terminal-user">harshith</span>@<span class="terminal-host">macbook</span>:<span class="terminal-path">~</span>$</span>
            </div>
          </div>
        </div>
      </div>
      <div class="slide-below">
        <div class="slide-below-inner" style="width:90vw; height:90vh; display:flex; align-items:stretch; justify-content:center; flex-direction:row; gap:2vw; box-sizing:border-box; padding:1vw 3vw 5vw 3vw;">
          <div class="side-box" style="flex:1; min-width:220px; max-width:350px; height:100%; box-sizing:border-box; display:flex; flex-direction:column; align-items:center; justify-content:flex-start; margin:0; padding:1.2em 1.1em 1.2em 1.1em;">
            <h3 style="margin:1.2em 0 1.2em 0; font-size:1.3em; letter-spacing:1px; text-align:center;">Open Source Contributions</h3>
            <div style="width:100%; display:flex; flex-direction:column; align-items:center; padding:1.1em 0.9em 1.1em 0.9em;">
              <div style="font-size:1.08em; color:#b39ddb; font-weight:600; margin-bottom:0.5em; text-align:center;">Apache Ambari</div>
              <ul style="list-style:none; padding:0; margin:0; display:flex; flex-direction:column; gap:0.7em; width:100%;">
                <li style="padding:0.6em 0.5em;"><a href="https://github.com/apache/ambari/pull/3848" target="_blank" style="color:#7c4dff; font-weight:500; text-decoration:underline;">AMBARI-26186 : Add Java 17 support to getJavaVersion method</a></li>
                <li style="padding:0.6em 0.5em;"><a href="https://github.com/apache/ambari/pull/4029" target="_blank" style="color:#7c4dff; font-weight:500; text-decoration:underline;">AMBARI-26520 : Fix deprecated usage string.uppercase</a></li>
                <li style="padding:0.6em 0.5em;"><a href="https://github.com/apache/ambari/pull/4033" target="_blank" style="color:#7c4dff; font-weight:500; text-decoration:underline;">AMBARI-26522 : Fix imports for collections</a></li>
                <li style="padding:0.6em 0.5em;"><a href="https://github.com/apache/ambari/pull/4034" target="_blank" style="color:#7c4dff; font-weight:500; text-decoration:underline;">AMBARI-26524: Fix inspect deprecations for Python 3.11 compatibility</a></li>
              </ul>
            </div>
          </div>
          <div class="side-box" style="flex:1; min-width:220px; max-width:350px; height:100%; box-sizing:border-box; display:flex; flex-direction:column; align-items:center; justify-content:flex-start; margin:0; padding:1.2em 1.1em 1.2em 1.1em;">
            <h3 style="margin:1.2em 0 1.2em 0; font-size:1.3em; letter-spacing:1px; text-align:center;">Certifications</h3>
            <div style="display:flex; flex-direction:column; gap:1.1em; width:100%; align-items:center; padding:1.1em 0.9em 1.1em 0.9em;">
              <a href="https://drive.google.com/file/d/1DRx0W-VPDhYyKn6Tfauu_rHUVgPttje9/view?usp=sharing" target="_blank" style="text-align:center; display:flex; align-items:center; gap:1em; padding:0.6em 0.5em; width:100%;">
                <img src="https://raw.githubusercontent.com/cncf/artwork/master/projects/kubernetes/icon/color/kubernetes-icon-color.png" alt="Kubernetes Certified Application Developer" style="width:70px; height:70px; object-fit:contain; border-radius:0.5em; box-shadow:0 2px 8px #7c4dff55; background:#fff;" />
                <div style="font-size:1em; color:#b39ddb; font-weight:600;">Kubernetes Certified Application Developer</div>
              </a>
              <a href="https://drive.google.com/file/d/10hnylToYbpVM0HXFWrkzhGoljfscKImB/view?usp=drive_link" target="_blank" style="text-align:center; display:flex; align-items:center; gap:1em; padding:0.6em 0.5em; width:100%;">
                <img src="https://seeklogo.com/images/P/postman-logo-0087CA0D15-seeklogo.com.png" alt="API based Programming" style="width:70px; height:70px; object-fit:contain; border-radius:0.5em; box-shadow:0 2px 8px #7c4dff55; background:#fff;" />
                <div style="font-size:1em; color:#b39ddb; font-weight:600;">Postman “API based Programming”</div>
              </a>
              <a href="https://drive.google.com/file/d/1_JsMNgtkCrcHcTih8TLIBH2A0eNjm3uf/view?usp=drive_link" target="_blank" style="text-align:center; display:flex; align-items:center; gap:1em; padding:0.6em 0.5em; width:100%;">
                <img src="https://www.python.org/static/community_logos/python-logo.png" alt="The Complete Python Bootcamp" style="width:70px; height:70px; object-fit:contain; border-radius:0.5em; box-shadow:0 2px 8px #7c4dff55; background:#fff;" />
                <div style="font-size:1em; color:#b39ddb; font-weight:600;">The Complete Python Bootcamp</div>
              </a>
              <a href="https://drive.google.com/file/d/1iPZg1Tj0-X7MFuO5nKhPRKYgT5nEF2aN/view?usp=drive_link" target="_blank" style="text-align:center; display:flex; align-items:center; gap:1em; padding:0.6em 0.5em; width:100%;">
                <img src="https://upload.wikimedia.org/wikipedia/commons/a/af/Tux.png" alt="Linux Administration Bootcamp" style="width:70px; height:70px; object-fit:contain; border-radius:0.5em; box-shadow:0 2px 8px #7c4dff55; background:#fff;" />
                <div style="font-size:1em; color:#b39ddb; font-weight:600;">Linux Administration Bootcamp</div>
              </a>
            </div>
          </div>
          <div class="side-box" style="flex:1; min-width:220px; max-width:350px; height:100%; box-sizing:border-box; display:flex; flex-direction:column; align-items:center; justify-content:flex-start; margin:0;">
            <h3 style="margin:1.2em 0 1.2em 0; font-size:1.3em; letter-spacing:1px; text-align:center;">Projects</h3>
          </div>
        </div>
      </div>
    </div>
    <div class="scroll-tube">
      <div class="scroll-tube-bar">
        <div class="scroll-tube-highlight"></div>
      </div>
    </div>
  </body>
  <script>
    // Terminal time clock
    function updateTerminalTime() {
      const el = document.getElementById('terminalTime');
      if (!el) return;
      const now = new Date();
      let hours = now.getHours();
      const minutes = now.getMinutes();
      const seconds = now.getSeconds();
      const ampm = hours >= 12 ? 'PM' : 'AM';
      hours = hours % 12;
      hours = hours ? hours : 12; // 0 should be 12
      const pad = n => n.toString().padStart(2, '0');
      const timeStr = `${pad(hours)}:${pad(minutes)}:${pad(seconds)} ${ampm}`;
      el.textContent = timeStr;
    }
    setInterval(updateTerminalTime, 1000);
    updateTerminalTime();

    // --- Terminal logic ---
    const terminalContent = document.querySelector('.terminal-content');
    // Directory structure as a JSON object
    const fileSystem = {
      projects: {
        'my-website': {},
        'cli-tool': {},
        'README.md': null
      },
      interests: {
        'ai': {},
        'music': {},
        'README.md': null
      },
      certifications: {
        'aws.pdf': null,
        'gcp.pdf': null
      },
      'about.txt': null
    };

    // Helper to get directory contents
    function getDirContents(pathArr, fs) {
      let node = fs;
      for (const part of pathArr) {
        if (node && typeof node === 'object' && part in node) {
          node = node[part];
        } else {
          return null;
        }
      }
      return node;
    }

    let fakePath = '~';
    let currentPath = [];

    function listDir(pathArr) {
      const node = getDirContents(pathArr, fileSystem);
      if (!node || typeof node !== 'object') return '';
      return Object.keys(node).map(name => {
        if (node[name] && typeof node[name] === 'object') {
          // Directory: color purple
          return `<span style="color:#b39ddb">${name}/</span>`;
        } else {
          // File: default color
          return `<span style="color:#e0e0e0">${name}</span>`;
        }
      }).join('    ');
    }

    const commands = {
      ls: () => listDir(currentPath),
      pwd: () => '/Users/harshith' + (currentPath.length ? '/' + currentPath.join('/') : ''),
      whoami: () => 'harshith',
      clear: () => { terminalContent.innerHTML = ''; addNewPrompt(); return ''; },
      help: () => 'Available commands: ls, pwd, whoami, clear, help, echo, date, cd, cat, exit, history',
      echo: (args) => args.join(' '),
      date: () => new Date().toString(),
      cd: (args) => {
        if (!args.length || args[0] === '~') {
          currentPath = [];
          fakePath = '~';
          return '';
        }
        if (args[0] === '..') {
          if (currentPath.length > 0) currentPath.pop();
          fakePath = '~' + (currentPath.length ? '/' + currentPath.join('/') : '');
          return '';
        }
        const node = getDirContents([...currentPath, args[0]], fileSystem);
        if (node && typeof node === 'object') {
          currentPath.push(args[0]);
          fakePath = '~/' + currentPath.join('/');
          return '';
        } else {
          return `cd: no such file or directory: ${args[0]}`;
        }
      },
      cat: (args) => {
        const node = getDirContents([...currentPath, args[0]], fileSystem);
        if (node === null) return `Contents of ${args[0]}...`;
        if (node && typeof node === 'object') return `cat: ${args[0]}: Is a directory`;
        return `cat: ${args[0] || ''}: No such file or directory`;
      },
      exit: () => 'logout',
    };

    function createPromptLine(input = '') {
      const line = document.createElement('div');
      line.className = 'terminal-line';
      line.innerHTML = `<span class="terminal-prompt">&nbsp;<span class="terminal-user">harshith</span>@<span class="terminal-host">macbook</span>:<span class="terminal-path">${fakePath}</span>$</span>&nbsp;<span class="terminal-input" contenteditable="true" spellcheck="false">${input}</span>`;
      return line;
    }

    function addNewPrompt() {
      // Make all previous inputs readonly
      const allInputs = terminalContent.querySelectorAll('.terminal-input');
      allInputs.forEach(el => el.contentEditable = 'false');
      // Add new prompt
      const newLine = createPromptLine();
      terminalContent.appendChild(newLine);
      const inputEl = newLine.querySelector('.terminal-input');
      inputEl.focus();
      scrollTerminalToBottom();
    }

    function scrollTerminalToBottom() {
      terminalContent.scrollTop = terminalContent.scrollHeight;
    }

    // On page load, clear terminal and add a fresh prompt
    window.addEventListener('DOMContentLoaded', () => {
      terminalContent.innerHTML = '';
      addNewPrompt();
    });

    // On page load, ensure only the last prompt is editable and has blinking cursor
    (function ensureSinglePrompt() {
      const lines = terminalContent.querySelectorAll('.terminal-line');
      lines.forEach((line, idx) => {
        const input = line.querySelector('.terminal-input');
        if (input) input.contentEditable = (idx === lines.length - 1) ? 'true' : 'false';
      });
      // Focus last input
      const lastInput = terminalContent.querySelector('.terminal-line:last-child .terminal-input');
      if (lastInput) lastInput.focus();
    })();

    // Command execution
    function runCommand(cmd) {
      const [base, ...args] = cmd.trim().split(/\s+/);
      if (!base) return '';
      if (commands[base]) {
        return commands[base](args);
      } else {
        return `${base}: command not found`;
      }
    }

    // --- Command history ---
    let commandHistory = [];
    let historyIndex = -1;

    // Listen for Enter key and arrow keys on input
    terminalContent.addEventListener('keydown', function(e) {
      const inputEl = terminalContent.querySelector('.terminal-line:last-child .terminal-input');
      if (!inputEl || document.activeElement !== inputEl) return;
      // Up/Down arrow for history
      if (e.key === 'ArrowUp' || e.key === 'ArrowDown') {
        e.preventDefault();
        if (commandHistory.length === 0) return;
        if (historyIndex === -1) historyIndex = commandHistory.length;
        if (e.key === 'ArrowUp') {
          if (historyIndex > 0) historyIndex--;
        } else if (e.key === 'ArrowDown') {
          if (historyIndex < commandHistory.length) historyIndex++;
        }
        if (historyIndex >= 0 && historyIndex < commandHistory.length) {
          inputEl.textContent = commandHistory[historyIndex];
        } else {
          inputEl.textContent = '';
        }
        // Move cursor to end
        const range = document.createRange();
        const sel = window.getSelection();
        range.selectNodeContents(inputEl);
        range.collapse(false);
        sel.removeAllRanges();
        sel.addRange(range);
        return;
      }
      // Enter key for command
      if (e.key === 'Enter') {
        e.preventDefault();
        const value = inputEl.textContent.trim();
        if (value) {
          commandHistory.push(value);
        }
        historyIndex = -1;
        inputEl.contentEditable = 'false';
        // Show output if any
        let output = runCommand(value);
        if (value === 'history') {
          // For each command, create a new output line so each is on its own line and scrolls like a real terminal
          commandHistory.forEach((cmd, i) => {
            const histLine = document.createElement('div');
            histLine.className = 'terminal-line';
            histLine.innerHTML = `<span style="margin-left:calc(1.1em + 0.2em)">${i + 1}  ${cmd}</span>`;
            terminalContent.appendChild(histLine);
          });
          scrollTerminalToBottom();
        } else if (output) {
          const outLine = document.createElement('div');
          outLine.className = 'terminal-line';
          outLine.innerHTML = `<span style="margin-left:calc(1.1em + 0.2em); white-space: pre-line">${output}</span>`;
          terminalContent.appendChild(outLine);
          scrollTerminalToBottom();
        }
        if (value === 'clear') return; // clear already handled
        addNewPrompt();
      }
    });

    // Autofocus on terminal input when clicking anywhere in the terminal area
    terminalContent.addEventListener('mousedown', function(e) {
      setTimeout(() => {
        const inputEl = terminalContent.querySelector('.terminal-line:last-child .terminal-input');
        if (inputEl) inputEl.focus();
      }, 0);
    });

    // Scroll tube highlight logic (for snap-container)
    function updateScrollTube() {
      const tube = document.querySelector('.scroll-tube');
      const snap = document.querySelector('.snap-container');
      if (!tube || !snap) return;
      const scrollY = snap.scrollTop;
      const vh = snap.clientHeight;
      // If in top slide, highlight top; if in second slide, highlight bottom
      if (scrollY < vh/2) {
        tube.classList.remove('highlight-bottom');
      } else {
        tube.classList.add('highlight-bottom');
      }
    }
    const snap = document.querySelector('.snap-container');
    if (snap) {
      snap.addEventListener('scroll', updateScrollTube);
      snap.addEventListener('resize', updateScrollTube);
    }
    window.addEventListener('resize', updateScrollTube);
    document.addEventListener('DOMContentLoaded', updateScrollTube);
  </script>
</html>
